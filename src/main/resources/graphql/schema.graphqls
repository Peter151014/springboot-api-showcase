type Book{
    id:ID,
    title:String,
    pages:Int
}

input BookInput{
    title:String,
    pages:Int
}

type Query{
    allBooks:[Book]
    getBook(bookId:Int):Book
}

type Mutation{
    createBook(book:BookInput):Book
}